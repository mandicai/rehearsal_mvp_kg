flask
flask-cors
sentence-transformers
spacy
torch
numpy
openai
python-dotenv
python-pptx
PyMuPDF
docling
requests
# Also requires LibreOffice (the `soffice` binary) on PATH for PPTX rendering
# in ingest/pptx_render.py - not pip-installable, e.g. `brew install --cask libreoffice`
